#include <stdio.h>
 
int main(){
 
      int i,j,a=0,b=0,N;
      scanf("%d",&N);
 
      char stone[N];
 
      scanf("%s",stone);
      
      for(i=0;stone[i] == '\0';i++){
            if(stone[i] == '#'

      if(a < b){
            printf("%d",a);
     }else{
            printf("%d",b);
      }
}
